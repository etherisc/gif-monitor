// Contracts
Contracts.join(Instances, "instance_id", "chain_data", []);
Contracts.join(Instances, "deployed_at_block", "chain_data", []);

// Addresses
Addresses.join(Instances, "chain_id", "", []);

